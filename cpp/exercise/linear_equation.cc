// 求二院一次方程的解,ax+by=c,其中a,b,c从键盘输入
// a>0,b>0,a>=b.

#include <iostream>

int main()
{
    int a, b, c;
    int x = 0, y = 0;
    std::cout << "Please input a,b,c (a>0,b>0,a>=b): " << std::endl;
    std::cin >> a >> b >> c;
    for ()
}
