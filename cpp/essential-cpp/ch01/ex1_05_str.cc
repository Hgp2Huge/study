#include <iostream>
#include <string>
using namespace std;

int main()
{
    // string class object
    string user_name;
    cout << "Please enter your name: ";
    cin >> user_name;

    if (1 < name_str.length())
        cout << "Hello, " << name_str << endl;
    else
        cout << "Error: the input must be at least two characters in length.\n";

    return 0;
}
